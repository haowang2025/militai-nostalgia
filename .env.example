# The browser always talks to the same-origin proxy.
VITE_NCM_BASE=/api

# Cloudflare Pages environment variables (configure in dashboard, never expose to Vite):
# NCM_ORIGIN=https://your-protected-ncm-origin.example.com
# NCM_SECRET=replace-with-a-long-random-secret
# NCM_REAL_IP=optional-China-IP-for-remote-hosting
